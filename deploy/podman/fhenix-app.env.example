# Copy to ~/.config/fhenix-app/fhenix-app.env on the server (chmod 600).
NODE_ENV=production
PORT=8787
HOSTNAME=0.0.0.0
NEXT_TELEMETRY_DISABLED=1

# Optional: override defaults baked into the client bundle at image build time
# NEXT_PUBLIC_ENCRYPTED_VOTING_ADDRESS=0xa28f103de761fbf88CE69Ac813A5F906F83c75f3
# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
